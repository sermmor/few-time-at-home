import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('App smoke test', (tester) async {
    // Placeholder — ftahApp uses platform services (Supabase, window_manager)
    // that require mocking for widget tests.
    expect(true, isTrue);
  });
}
