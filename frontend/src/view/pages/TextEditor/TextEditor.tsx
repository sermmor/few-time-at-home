/**
 * Text Editor — powered by BlogXtender.
 * The old simple textarea editor has been replaced by the full BlogXtender editor.
 * Files opened from the Cloud browser are loaded via TemporalData on first mount.
 */
import { BlogEditor } from '../BlogEditor/BlogEditor';

export const TextEditor = BlogEditor;
